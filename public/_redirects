# Ensure API calls proxy to backend (must come first)
/api/*  https://trustique-assist.onrender.com/api/:splat  200

# Handle static assets
/assets/*  /assets/:splat  200
/*.css  /*.css  200
/*.js   /*.js   200
/*.png  /*.png  200
/*.jpg  /*.jpg  200
/*.svg  /*.svg  200

# SPA routing fallback - serve index.html for all other routes
/*    /index.html   200